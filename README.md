# ui-elements

A collection of code snippets and minimal implementations in HTML, CSS and JS of usual UI elements. Code here is not production-ready, but offers viable solutions.
Each subdirectory contains a unique UI element. 
